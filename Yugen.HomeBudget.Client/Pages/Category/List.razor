@attribute [Route(PageConstants.Category)]
@using Yugen.HomeBudget.Shared.Contants
<!-- AllowAnonymous / Authorize -->
@attribute [Authorize]

<!-- ========== title-wrapper start ========== -->
<TitleComponent PageTitle="@PageConstants.Category"
                Title="@PageConstants.Category"
                Breadcrumb="@PageConstants.Category" />
<!-- ========== title-wrapper end ========== -->
@if (_categories == null)
{
    <p><em>Loading...</em></p>
}
else
{
    <div class="row">
        <div class="card-style mb-30">
            <div class="title d-flex flex-wrap align-items-center justify-content-between ">
                <div class="left">
                    <a href="@PageHelper.AddHref(PageConstants.Category)"
                       class="main-btn primary-btn btn-hover btn-sm"
                       title="Add">
                        <i class="bi bi-plus mr-5"></i> Add
                    </a>
                </div>
                <PagerComponent PageIndex=@_paginatedList.PageIndex
                            TotalPages=@_paginatedList.TotalPages
                            OnClick="PageIndexChanged"
                            HasNextPage=@_paginatedList.HasNextPage
                            HasPreviousPage=@_paginatedList.HasPreviousPage />
            </div>
        </div>
    </div>
    <div class="row">
        @foreach (var category in _categories)
        {
            <div class="col-xl-3 col-lg-4 col-sm-6">
                <div class="card-style-3 mb-30">
                    <div class="card-content">
                        <h4>@category.Title</h4>
                        <ul class="text-sm">
                            @foreach (var subCategory in category.SubCategoriesDto)
                            {
                                <li class="bi bi-chevron-right">@subCategory.Title</li>
                            }
                        </ul>
                        @*
                <small>@category.CreatedOn</small>
                <small>@category.LastModifiedOn</small>
                <small>@category.CreatedByApplicationUserId</small>
                <small>@category.LastModifiedByApplicationUserId</small>
                *@

                        <a href="@PageHelper.EditHref(PageConstants.Category, category.Id)"
                           class="main-btn warning-btn btn-hover btn-sm"
                           title="Edit">
                            <i class="bi bi-pencil"></i>
                        </a>

                        <DeleteComponent Id="@category.Id"
                                 DeleteCallback="@DeleteAsync" />
                    </div>
                </div>
            </div>
        }
    </div>
}